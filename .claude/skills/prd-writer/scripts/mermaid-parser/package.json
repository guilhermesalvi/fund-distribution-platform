{
  "name": "prd-writer-mermaid-parser",
  "private": true,
  "description": "Deterministic mermaid.parse() runner (Node + jsdom, no Chromium) used by lint_mermaid.py. Versions are pinned: bump deliberately.",
  "type": "module",
  "dependencies": {
    "jsdom": "30.0.1",
    "mermaid": "11.17.2"
  }
}
